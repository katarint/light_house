
# defines the function light_house , input parameters : (x_0,y_0,N)
# x,y= position of light house, N=number of sampled data

import numpy as np

def light_house(x_0, y_0, N):


    np.array=np.random.uniform(0, np.pi, size=N)

    return;

light_house(1,1,5)

print('*********************************************************************************************************')
print('data samples (in angles):',np.array)